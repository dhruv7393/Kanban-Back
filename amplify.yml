version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - npm install

    build:
      commands:
        - npm run build
        - npm run postbuild
        - echo "MONGODB_URI=mongodb+srv://dhruvaws8:Shleeji7393i@cluster0.znyjnot.mongodb.net/Kanban?retryWrites=true&w=majority" >> .amplify-hosting/compute/default/.env
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"
